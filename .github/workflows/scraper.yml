name: Hourly Civic RSS Scraper

on:
  # Runs at minute 0 past every hour
  schedule:
    - cron: '0 * * * *'
  
  # Allows you to click a button in GitHub to run it manually
  workflow_dispatch:

jobs:
  scrape-and-push:
    runs-on: ubuntu-latest

    # This globally forces the runner to use Node 24, silencing the deprecation warning. Just a future proof measure since Node.js 20 will be removed from the runner on September 16th, 2026
    env:
      FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: true

    steps:
      - name: Checkout Repository
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11' # Matches a stable, modern Python version
          cache: 'pip'

      - name: Install Dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r scraper/requirements.txt

      - name: Execute Scraper Script
        env:
          # This securely injects the secret you saved in GitHub into the runner
          DATABASE_URL: ${{ secrets.DATABASE_URL }}
        run: python scraper/rss_scraper.py